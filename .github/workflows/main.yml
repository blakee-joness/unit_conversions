name: Validate PR
on:
  pull_request:
    branches: [ main ]
  push: 
jobs:
  check_formatting:
    runs-on: ubuntu-latest
    name: Check Formatting
    steps:
      - uses: 'node20'
      - run: |
          dotnet tool restore
          dotnet csharpier --check .
